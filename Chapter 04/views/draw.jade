html 
    head 
        body 
            - for (var i = 0; i< row_number; i ++)
                // Call an empty line to store all symbols in 1 line
                - var line = '';
                - for (var j = 0; j < col_number; j ++)
                    - line = line + " " + symbol
                // print whole line out
                .p= line